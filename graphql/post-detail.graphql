query PostDetail($id: Int!) {
    post(where: { id: $id }) {
        id
        title
        content
        slug
        excerpt
        draft
        createdAt
        updatedAt
        author {
            id
            name
        }
    }
}
